import 'package:flutter/material.dart';

class Contact {
  final String id, name, initials;
  final Color color;
  const Contact({required this.id, required this.name, required this.initials, required this.color});
}
